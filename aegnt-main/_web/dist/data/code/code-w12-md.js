window.__code__ = window.__code__ || {};
window.__code__["code-w12-md"] = {"code_id": "code-w12-md", "kind": "local", "title": "讲稿.md", "source_path": "/Users/zhongyou/Desktop/2027年/code/w12/讲稿.md", "highlight_line": null, "code_html": "<pre># W12 · 5 分钟讲清这个项目\n\n&gt; 面试用。按这个结构口语讲，别背稿。每段控制在对应时间。\n\n## 1. 一句话定位（30 秒）\n\n「我基于 agentscope 2.0 做了一个**研究-写作-审校多 Agent 协作平台**：用户提需求，Leader 协调 Researcher 查资料、Writer 写稿、Reviewer 审校，最后聚合输出。平台自带一个我**自建的 MCP Server** 作为 Researcher 的数据源，还接了预算门控、Token 监控和 SSE 服务化。」\n\n## 2. 为什么这样设计（1 分钟）\n\n「我特意没用 1.0 那套。agentscope 2.0 把 1.0 的 ReActAgent/DialogAgent 子类和 Pipeline/MsgHub 全砍了——只剩一个统一 Agent，行为靠组合（plug model/toolkit/middleware/config）表达。多 Agent 也不再用代码 pipeline，而是 Leader 用服务层 Team 工具动态调度。\n\n这么设计的好处：编排由模型按任务动态决策（资料不够再查、稿差再审），比写死 `pipeline([a,b,c])` 灵活、可扩展、能多租户。这也是我为什么选它而不是直接上低代码——我要做企业级治理和深度扩展。」\n\n## 3. 技术亮点（挑 3 个深的讲，2 分钟）\n\n**① 自建 MCP Server（核心）**\n「Researcher 不查本地函数，而是调我自建的 MCP Server（`mcp_server/server.py`）。这个 Server 有企业级六件套：工具注册（FastMCP 装饰器自动出 schema）、鉴权（token 校验）、超时、调用审计（记谁/何时/调什么/结果）、危险 URL 拦截（防 SSRF/读本地文件）。agentscope 当 MCP 客户端，用 MCPClient stdio 拉起 Server、`list_tools` 动态发现工具。这块对照了 QwenPaw 的 `app/routers/mcp.py` 的策略/白名单/审计设计。」\n\n**② Loop Engineering 治理**\n「我手写 ReAct 时就撞上过死循环——所以这里我借鉴 QwenPaw 的 StopGate 做了 BudgetGate：用 on_model_call 中间件累计 token，超预算注入停止指令 + max_iters 兜底。这比单靠 max_iters 精准，对应 JD 里&quot;Agent Loop 治理、解决无效循环/执行失控&quot;。」\n\n**③ Runtime + SSE 工业化**\n「框架的 reply_stream 是&#x27;原料&#x27;，我仿 QwenPaw 写了 MiniRuntime（3 阶段，鉴权/限流/审计可挂各阶段 hook）+ Envelope（带 seq 的 SSE 状态机），包成 FastAPI 的 `/stream` SSE 端点。这层是&#x27;会用框架&#x27;和&#x27;会做产品&#x27;的分水岭。」\n\n## 4. 踩过的坑 + 怎么解（1 分钟）\n\n「三个坑，正好是框架替你解决的：\n- **上下文爆炸**：手写时历史越跑越长——用框架结构化压缩（超 80% 触发，压成五字段任务进度档案，不是简单截断）。\n- **工具结果过长**：`tool_result_limit` 截断 + 可 offloader 卸载。\n- **死循环**：上面说的 BudgetGate。\n这几个坑我都是先自己撞上、再去看框架怎么解的——所以理解得比较深。」\n\n## 5. 可观测（30 秒）\n\n「Token 监控用 ModelWrapper 包装记每次用量；审计在 MCP Server 和工具调用全程记录；Trace 用 agentscope 的 TracingMiddleware 出 agent/llm/tool 三类 span。」\n\n## 6. 局限与下一步（30 秒，诚实加分）\n\n「现在 leader 协调在单进程里是顺序流，下一步要升级成真异步团队——用 agentscope 服务层 TeamCreate/TeamSay + Redis MessageBus，这样能多 worker 横向扩展、多租户。沙箱层面现在只有危险 URL 拦截，还没接内核级隔离（QwenPaw 的 Seatbelt/bubblewrap），也是下一步。」\n\n---\n\n## 可能的追问预答\n\n- **Q：和 LangChain/dify 比？** → 要深度可控+企业治理选自建框架，求快选低代码。我选前者因为要做治理与扩展。\n- **Q：多租户怎么做？** → MessageBus 换 Redis、SessionService 多租户隔离、Token 按租户配额、MCP 鉴权带 scope、沙箱隔离。\n- **Q：为什么用 DashScope？** → 国内易拿 key、agentscope 原生支持好、换 provider 业务代码不改（Formatter 隔离）。\n- **Q：2.0 和 1.0 最大区别？** → 1.0 写死子类+pipeline，2.0 统一 Agent+组合+事件流+服务层 Team。&quot;为日益强大的 agentic LLM 设计&quot;——不约束模型，利用它自身推理。</pre>", "lines": 49, "explanation": "讲稿.md 是本教程的可运行示例代码，示范对应周次核心概念；顶部注释含运行前置依赖与预期输出，建议结合左侧源码与对应周文档「动手作业」一起读。", "knowledge_points": [], "related_docs": ["W12-多Agent协作平台实战(下)-毕业交付"]};
