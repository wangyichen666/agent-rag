window.__docs__ = window.__docs__ || {};
window.__docs__["01-Agent范式"] = {"doc_id": "01-Agent范式", "title": "面试问答卡 01 · Agent 范式", "stage": "面试卡", "html": "<h1>面试问答卡 01 · Agent 范式</h1>\n\n<p>> 覆盖：W2 / W3 / W8。对应 JD：ReAct/Plan-Execute/Multi-Agent/CoT/ToT/Self-Consistency。</p>\n\n<p>---</p>\n\n<h2>Q1：讲讲你知道的 Agent 范式，ReAct、Plan-and-Execute、Multi-Agent 各是什么？</h2>\n\n<h3>【模范回答】</h3>\n\n<p>我先把这些概念分个层，免得混在一起。其实面试官常把三个层次的东西混着问——<strong>Prompt 技术</strong>（Few-shot、CoT、ToT、Self-Consistency，研究的是「怎么问模型」）、<strong>Agent 范式</strong>（ReAct、Plan-Execute、Multi-Agent，研究的是「怎么让模型行动」）、<strong>Agent 系统</strong>（多 Agent 怎么协作、怎么服务化、怎么做工程治理）。Prompt 技术不是 Agent 范式，它是范式里用到的手法。</p>\n\n<p>先说 <strong>ReAct</strong>，Reasoning + Acting。它的核心是「想一步、做一步、看一步」交替进行：模型先输出一段 Thought（我的推理），再输出一个 Action（我要调哪个工具、传什么参数），执行工具拿到 Observation 后，模型再基于这个结果想下一步。它最大的优点是<strong>灵活</strong>——边走边看，适合探索性、链路不长的任务。缺点是<strong>没有全局计划</strong>，长任务容易「漂」，一步步跳跃式地走偏。现在主流框架默认都是 ReAct，agentscope 2.0 也不例外，它的推理-行动循环就内建在统一 Agent 的 <code class=\"inline\">_reply_impl</code> 里，默认最多 20 轮迭代。</p>\n\n<p>然后是 <strong>Plan-and-Execute</strong>。它和 ReAct 的差别在于前面多了一个「规划」阶段：先让模型<strong>一次性把任务拆成一份完整的 JSON 计划</strong>（第 1 步干什么、第 2 步干什么……），然后再逐步执行这份计划；更关键的是执行中如果发现某步结果不对，可以<strong>回头重新规划（replan）</strong>。所以它有个「先分解、后执行、可修正」的闭环。它适合<strong>长任务、目标明确</strong>的场景，比如「写一份调研报告」这种多步骤任务，先有全局计划能避免 ReAct 那种逐步漂移。代价是计划阶段要花额外 token，而且初始计划可能就是错的，所以 replan 机制很重要。</p>\n\n<p>最后是 <strong>Multi-Agent</strong>，多智能体协作。它不再是单个 Agent 干所有事，而是<strong>多个分工的 Agent 各司其职</strong>。通信上主要有三种模式：一是<strong>消息传递</strong>，Agent 之间点对点发消息；二是<strong>共享内存 / 黑板模式</strong>，所有 Agent 读写同一块「黑板」，谁有结果往上传、谁需要谁来读；三是 <strong>Leader-Worker + 消息总线</strong>，一个 Leader 负责拆任务、调度，多个 Worker 各自执行，通过一个消息总线解耦收发——agentscope 2.0 采用的就是这种。</p>\n\n<p>实际工程里这三种范式<strong>经常混用</strong>，不是非此即彼。最常见的组合是：先用 Plan-and-Execute 出一份大纲，然后每个子步骤用 ReAct 执行；如果任务复杂到需要不同角色，再上 Multi-Agent，而 Multi-Agent 里的每个 Worker 内部又可以是 ReAct。我自己的毕业项目就是 Leader 拆任务、Researcher（查资料）、Writer（写稿）、Reviewer（审校）三个 Worker 协作，每个 Worker 内部都是 ReAct 循环。</p>\n\n<p>> <strong>要点速记</strong>：① 分三层（Prompt 技术/Agent 范式/Agent 系统）别混；② ReAct 边走边看灵活但易漂；③ Plan-Execute 先规划可 replan 防长任务漂；④ Multi-Agent 三种通信（消息传递/黑板/Leader-Worker+总线）；⑤ 常混用：Plan 出大纲 + ReAct 执行 + Multi-Agent 分工。</p>\n<p>></p>\n<p>> <strong>源码佐证</strong>：agentscope ReAct 内建于 <code class=\"inline\">Agent._reply_impl</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"src-agentscope-agent-agent-py-664\" data-ref=\"src/agentscope/agent/_agent.py:664\">src/agentscope/agent/_agent.py:664</a></code>），<code class=\"inline\">max_iters</code> 默认 20（<code class=\"inline\"><a class=\"code-link\" data-code=\"agent-config-py-126\" data-ref=\"agent/_config.py:126\">agent/_config.py:126</a></code>）；多 Agent 用服务层 Team 工具 <code class=\"inline\"><a class=\"code-link\" data-code=\"app-tool-team-create-py\" data-ref=\"app/_tool/_team_create.py\">app/_tool/_team_create.py</a></code>/<code class=\"inline\"><a class=\"code-link\" data-code=\"team-say-py\" data-ref=\"_team_say.py\">_team_say.py</a></code> + <code class=\"inline\">app/message_bus/</code>。</p>\n<p>></p>\n<p>> <strong>压轴一句话</strong>：ReAct 灵活易漂、Plan-Execute 防漂、Multi-Agent 分工，三者常混用——Plan 出大纲、ReAct 执行每步、Multi-Agent 分角色协作。</p>\n\n<p>---</p>\n\n<h2>Q2：Few-shot、CoT、ToT、Self-Consistency 分别是什么？什么时候用？</h2>\n\n<h3>【模范回答】</h3>\n\n<p>这四个都是 <strong>Prompt 技术</strong>，不是 Agent 范式——它们研究的是「怎么把问题问好」，让模型在还没开始调工具之前就能想得更对。</p>\n\n<p><strong>Few-shot</strong> 最朴素：给模型几个「输入→输出」的示例，让它照着模式做。它解决的是「格式不确定」或「模型对任务不太懂」的问题。比如你要让它抽结构化字段，给两三个例子比写一堆规则管用。代价是示例占 token，所以示例要精。</p>\n\n<p><strong>CoT</strong>，Chain-of-Thought，是最划算的一个。做法很简单——在 prompt 里加一句「让我们一步步想」（Let's think step by step），让模型先展开推理过程再给答案。它对<strong>复杂推理</strong>任务（数学、逻辑、多步推断）几乎总能提分，而且几乎不增加成本，所以我称它「免费午餐」。原理是：模型是自回归逐 token 生成的，先输出推理步骤相当于给自己搭脚手架，最后的答案就站在这些步骤上，比直接蹦答案稳。</p>\n\n<p><strong>ToT</strong>，Tree-of-Thoughts，是 CoT 的「重装版」。CoT 是一条链想到底，ToT 是让模型<strong>生成多条不同思路、对每条打分、保留高分、放弃低分、必要时回溯</strong>——像搜索树一样。它适合<strong>有明确评价标准的搜索/规划任务</strong>，比如解谜、博弈、路径规划，你能定义「这条路好不好」。但它的代价大：多分支、多轮、还要打分评估，token 消耗是 CoT 的好几倍。所以<strong>只用在真的需要搜索、且有打分标准的场景</strong>，普通任务用它纯属浪费。</p>\n\n<p><strong>Self-Consistency</strong> 是给 CoT 上的一道保险。CoT 偶尔还是不稳，思路走偏一次答案就错。Self-Consistency 的做法是：<strong>对同一个问题用 CoT 采样多次（调高 temperature），得到多个答案，然后投票取多数</strong>。它本质是用「多次采样 + 多数表决」给 CoT 降噪，适合那些答案离散、可对齐的任务（比如数学题答案是个数）。</p>\n\n<p>实操优先级我的排序是：<strong>CoT 几乎总开（免费午餐）→ Few-shot 当格式要稳时用 → Self-Consistency 当 CoT 还不够稳、且答案可对齐时用 → ToT 只在搜索类、有打分标准时用</strong>。这四个可以叠加，但每加一层都要算成本。</p>\n\n<p>> <strong>要点速记</strong>：① 都是 Prompt 技术非范式；② CoT 免费午餐、几乎总有效；③ ToT 贵、只在搜索类有打分标准时用；④ Self-Consistency = CoT + 多次采样投票；⑤ Few-shot 定格式。优先级 CoT > Few-shot > Self-Consistency > ToT。</p>\n<p>></p>\n<p>> <strong>源码佐证</strong>：CoT 在 prompt 层面 manual 引导；部分模型 provider 支持原生 thinking（如 DashScope <code class=\"inline\">DashScopeChatModel.Parameters.thinking_enable</code>，见 <code class=\"inline\"><a class=\"code-link\" data-code=\"model-dashscope-model-py-53\" data-ref=\"model/_dashscope/_model.py:53\">model/_dashscope/_model.py:53</a></code>）；结构化强约束可用 <code class=\"inline\">generate_structured_output</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"model-base-py-438\" data-ref=\"model/_base.py:438\">model/_base.py:438</a></code>）。</p>\n<p>></p>\n<p>> <strong>压轴一句话</strong>：CoT 是免费午餐几乎总开，ToT 贵且只用在搜索类，Self-Consistency 是给 CoT 加投票保险，Four 按「CoT→Few-shot→Self-Consistency→ToT」排性价比。</p>\n\n<p>---</p>\n\n<h2>Q3：Multi-Agent 有哪几种通信模式？上下文怎么隔离、结果怎么聚合？</h2>\n\n<h3>【模范回答】</h3>\n\n<p>Multi-Agent 通信主要有三种模式。第一种<strong>消息传递</strong>，Agent 之间点对点直接发消息，谁给谁发很明确，优点是简单直接，缺点是 Agent 多了会出现「N×N」的通信网，耦合重。第二种<strong>共享内存 / 黑板模式</strong>，所有 Agent 读写同一块公共区域——谁产出了结果往「黑板」上写，谁需要就来读，互相不直接通信。它的好处是解耦，Agent 之间不需要知道彼此存在；难点是「黑板」的读写要管并发和一致性。第三种是 <strong>Leader-Worker + 消息总线</strong>，一个 Leader 负责拆任务和调度，多个 Worker 执行，通信统一走一条「消息总线」中转——agentscope 2.0 就是这种，它用 <code class=\"inline\">TeamCreate</code>/<code class=\"inline\">AgentCreate</code>/<code class=\"inline\">TeamSay</code> 这些工具让 Leader 管理 Worker，消息走 MessageBus（内存版或 Redis 版）。</p>\n\n<p><strong>上下文隔离</strong>其实在这套架构里是天然成立的：每个 Worker 是独立的 session、独立的 context（消息历史），它们之间<strong>不共享对话历史</strong>。这点很关键，否则一个 Worker 把它冗长的推理历史塞给另一个，会污染对方的判断、还会把上下文撑爆。所以隔离是设计前提，不是可选优化。</p>\n\n<p>但光隔离不够，还得<strong>聚合</strong>。怎么聚合？核心原则是「Leader 只把必要的产物传给下游，不传全量历史」。举例，我的毕业项目里 Researcher 查完资料，Leader 不会把 Researcher 整段推理过程丢给 Writer，而只把「Researcher 产出的 3 条要点」这个<strong>结果</strong>传给 Writer；Writer 写完初稿，Leader 也只把初稿传给 Reviewer。每个 Worker 拿到的是上游的「提炼产物」，不是上游的「完整心智过程」。最后 Leader 把三份产物聚合成最终输出。</p>\n\n<p>为什么要这么做？一是省 token（不传冗长历史），二是防「串味」（不让 A 的发散思考干扰 B 的专注任务），三是可调试（每个环节产物清晰，坏了能定位）。这套「隔离上下文 + 只传必要产物 + Leader 聚合」就是工程上落地 Multi-Agent 的标准做法，对应 JD 里提到的「Agent 间的上下文隔离与结果聚合」。</p>\n\n<p>> <strong>要点速记</strong>：① 三种通信——消息传递（点对点，N×N 耦合）、黑板（共享内存，解耦但难管一致性）、Leader-Worker+总线（agentscope 采用）；② 上下文隔离天然成立（每 Worker 独立 session）；③ 聚合原则——只传必要产物不传全量历史；④ 省token+防串味+可调试；⑤ Leader 最后聚合。</p>\n<p>></p>\n<p>> <strong>源码佐证</strong>：Team 工具 <code class=\"inline\"><a class=\"code-link\" data-code=\"app-tool-team-create-py\" data-ref=\"app/_tool/_team_create.py\">app/_tool/_team_create.py</a></code>/<code class=\"inline\"><a class=\"code-link\" data-code=\"team-say-py\" data-ref=\"_team_say.py\">_team_say.py</a></code>；MessageBus <code class=\"inline\"><a class=\"code-link\" data-code=\"app-message-bus-in-memory-message-bus-py\" data-ref=\"app/message_bus/_in_memory_message_bus.py\">app/message_bus/_in_memory_message_bus.py</a></code>/<code class=\"inline\"><a class=\"code-link\" data-code=\"redis-message-bus-py\" data-ref=\"_redis_message_bus.py\">_redis_message_bus.py</a></code>；SubAgent 模板（定义独立 Worker 的 prompt/权限）<code class=\"inline\"><a class=\"code-link\" data-code=\"app-types-py\" data-ref=\"app/_types.py\">app/_types.py</a></code>。</p>\n<p>></p>\n<p>> <strong>压轴一句话</strong>：三种通信（点对点/黑板/Leader-Worker+总线），Worker 各自独立上下文天然隔离，Leader 只把必要产物传下游最后聚合——省 token、防串味、好调试。</p>", "toc": [{"level": 1, "text": "面试问答卡 01 · Agent 范式"}, {"level": 2, "text": "Q1：讲讲你知道的 Agent 范式，ReAct、Plan-and-Execute、Multi-Agent 各是什么？"}, {"level": 3, "text": "【模范回答】"}, {"level": 2, "text": "Q2：Few-shot、CoT、ToT、Self-Consistency 分别是什么？什么时候用？"}, {"level": 3, "text": "【模范回答】"}, {"level": 2, "text": "Q3：Multi-Agent 有哪几种通信模式？上下文怎么隔离、结果怎么聚合？"}, {"level": 3, "text": "【模范回答】"}], "prev": "W12-多Agent协作平台实战(下)-毕业交付", "next": "02-消息与编排", "path": "面试问答卡/01-Agent范式.md"};
