window.__docs__ = window.__docs__ || {};
window.__docs__["W04-agent scope入门与Agent内核"] = {"doc_id": "W04-agent scope入门与Agent内核", "title": "W04 · AgentScope 入门与 Agent 内核", "stage": "框架", "html": "<h1>W04 · AgentScope 入门与 Agent 内核</h1>\n\n<p>> 本周目标 | 跑通 quickstart，读透 <code class=\"inline\">Agent</code> 内核，理解 2.0 为什么只剩一个统一的 Agent 类。</p>\n<p>> JD 考点：Agent 运行时、推理循环（ReAct）、框架架构设计。</p>\n\n<h2>1. 本周你将搞懂什么</h2>\n\n<p>W03 你手写了 50 行 ReAct。现在把那个轮子和 agentscope 2.0 对照着看：框架替你做了什么、在哪做的、留了什么扩展点。</p>\n\n<p>本周只啃一个文件——<code class=\"inline\"><a class=\"code-link\" data-code=\"src-agentscope-agent-agent-py\" data-ref=\"src/agentscope/agent/_agent.py\">src/agentscope/agent/_agent.py</a></code>（2837 行，核心中的核心）。读完你会理解：为什么 2.0 砍掉了 1.0 的所有子类，只剩一个 <code class=\"inline\">Agent</code>。</p>\n\n<h2>2. 原理铺垫：2.0 的设计哲学</h2>\n\n<p>1.0 时代：<code class=\"inline\">ReActAgent</code>、<code class=\"inline\">DialogAgent</code>、<code class=\"inline\">UserAgent</code> 各是各的子类，行为写死在类里。想改一点只能继承重写。</p>\n\n<p>2.0 哲学：<strong>现代 LLM 自己就会推理和用工具</strong>（你在 W02 见过它\"演\"ReAct）。框架不该用类层次去规定行为，而该：</p>\n\n<ul>\n<li>提供一个<strong>统一的 <code class=\"inline\">Agent</code></strong>，内置 reasoning-acting 循环；</li>\n<li>行为差异通过<strong>组合</strong>（plug 进不同的 model / toolkit / middlewares / config）来表达，而不是继承。</li>\n</ul>\n\n<p>所以 2.0 的 <code class=\"inline\">Agent</code> 构造器是个\"装配车间\"，———— plug 进去什么，它就是什么 Agent：</p>\n\n<pre><code class=\"language-python\">\nAgent(\n    name, system_prompt, model,\n    toolkit=...,        # ← plug 不同工具 = 不同能力\n    middlewares=...,    # ← plug 不同中间件 = 不同行为(记忆/审计/限流...)\n    react_config=...,   # ← 调循环参数\n    context_config=..., # ← 调上下文压缩\n)\n\n</code></pre>\n\n<p>这和 1.0 的\"写死子类\"是本质区别。组合 > 继承，是 2.0 的核心心法。</p>\n\n<h2>3. 源码精读</h2>\n\n<h3>3.1 构造与装配（<code class=\"inline\"><a class=\"code-link\" data-code=\"agent-py-100\" data-ref=\"_agent.py:100\">_agent.py:100</a></code>）</h3>\n\n<p><code class=\"inline\">Agent.__init__</code>（<code class=\"inline\">:100</code>）签名已确认：</p>\n\n<pre><code class=\"language-\">\nname, system_prompt, model, toolkit?, middlewares?, state?, offloader?,\nmodel_config?, context_config?, react_config?\n\n</code></pre>\n\n<p>注意 <code class=\"inline\">:170-188</code>：middlewares 不是一股脑全挂，而是按 <code class=\"inline\">is_implemented(&quot;on_xxx&quot;)</code> <strong>筛选</strong>到六个列表里——只实现了 <code class=\"inline\">on_reasoning</code> 的中间件只在推理时跑，实现了 <code class=\"inline\">on_model_call</code> 的只在调模型时跑。这就是后面 W07 的\"洋葱模型\"基础。</p>\n\n<h3>3.2 三个入口方法（<code class=\"inline\">:194 / :225 / :266</code>）</h3>\n\n<table>\n<tr><th>方法</th><th>行号</th><th>作用</th></tr>\n<tr><td><code class=\"inline\">reply_stream(inputs)</code></td><td><code class=\"inline\">:194</code></td><td><strong>核心</strong>：异步生成器，边跑边吐 <code class=\"inline\">AgentEvent</code>。2.0 的主力入口</td></tr>\n<tr><td><code class=\"inline\">reply(inputs)</code></td><td><code class=\"inline\">:225</code></td><td>消费整个流，返回最终 <code class=\"inline\">Msg</code>（阻塞到完成）</td></tr>\n<tr><td><code class=\"inline\">observe(msgs)</code></td><td><code class=\"inline\">:266</code></td><td>只把消息塞进 context，不触发回复（\"观察\"）</td></tr>\n</table>\n\n<p><code class=\"inline\">reply_stream</code> 内部就是 <code class=\"inline\">async for chunk in self._reply(...): if not isinstance(chunk, Msg): yield chunk</code>（<code class=\"inline\">:221</code>）——把 <code class=\"inline\">_reply</code> 里的事件吐出来，把最终的 <code class=\"inline\">Msg</code> 留给 <code class=\"inline\">reply()</code> 用。</p>\n\n<h3>3.3 内核循环 <code class=\"inline\">_reply_impl</code>（<code class=\"inline\">:664</code>）</h3>\n\n<p>这是 W03 那 50 行轮子的\"豪华版\"，精确对应关系：</p>\n\n<pre><code class=\"language-\">\n_reply_impl (:664):\n  while 未结束 and iter &lt; max_iters:\n      action = _check_next_action()        # :2542  ← 你W03的&quot;还要不要调工具&quot;\n      if action == &quot;reasoning&quot;:\n          await compress_context()         # :759   ← W07上下文压缩\n          yield from _reasoning()          # :763   ← 你W03的&quot;调模型&quot;\n          batches = _batch_tool_calls()\n          yield from _execute_sequential_tool_calls()   # :1375  ← 你W03的&quot;执行工具&quot;\n          # 或 _execute_concurrent_tool_calls()        # :1425\n      else: break\n  yield ReplyEndEvent(...)\n\n</code></pre>\n\n<p>你 W03 写的 <code class=\"inline\">for i in range(max_iters)</code> ≈ 这里的循环；你的\"tool_calls 为空就结束\"≈ <code class=\"inline\">_check_next_action</code>；你的\"执行 + 回填\"≈ <code class=\"inline\">_execute_*_tool_calls</code>。</p>\n\n<p>框架多出来的（你 W03 没做、框架替你做了）：</p>\n<ul>\n<li><strong>并发分批</strong>：多个工具调用按 <code class=\"inline\">is_concurrency_safe</code> 分 sequential/concurrent 两批（<code class=\"inline\">:1375/:1425</code>）。</li>\n<li><strong>压缩</strong>：每轮推理前 <code class=\"inline\">compress_context()</code>（<code class=\"inline\">:759</code>），防上下文爆炸。</li>\n<li><strong>事件流</strong>：每个阶段 yield 事件（<code class=\"inline\">ReplyStartEvent</code> / <code class=\"inline\">TextBlockDeltaEvent</code> / <code class=\"inline\">ToolCallEndEvent</code> …），UI 能实时渲染。</li>\n<li><strong>人在回路/中断</strong>：<code class=\"inline\">RequireUserConfirmEvent</code>、<code class=\"inline\">UserInterruptEvent</code> 等。</li>\n</ul>\n\n<h3>3.4 三个 Config（<code class=\"inline\"><a class=\"code-link\" data-code=\"agent-config-py\" data-ref=\"agent/_config.py\">agent/_config.py</a></code>）</h3>\n\n<table>\n<tr><th>Config</th><th>行号</th><th>关键字段</th></tr>\n<tr><td><code class=\"inline\">ContextConfig</code></td><td><code class=\"inline\">:51</code></td><td><code class=\"inline\">trigger_ratio=0.8</code>（超 80% 上下文就压缩）、<code class=\"inline\">reserve_ratio=0.1</code>（留 10%）、<code class=\"inline\">tool_result_limit=50000</code>（工具结果超 5 万 token 截断）、<code class=\"inline\">summary_schema</code>/<code class=\"inline\">summary_template</code>（结构化压缩模板）</td></tr>\n<tr><td><code class=\"inline\">ReActConfig</code></td><td><code class=\"inline\">:123</code></td><td><code class=\"inline\">max_iters</code>（默认 20，循环上限）、<code class=\"inline\">stop_on_reject</code>、<code class=\"inline\">interruption_message</code></td></tr>\n<tr><td><code class=\"inline\">ModelConfig</code></td><td><code class=\"inline\">:164</code></td><td><code class=\"inline\">max_retries</code>、<code class=\"inline\">fallback_model</code>（主模型挂了切备模型）</td></tr>\n</table>\n\n<h3>3.5 SummarySchema（<code class=\"inline\"><a class=\"code-link\" data-code=\"config-py-9\" data-ref=\"_config.py:9\">_config.py:9</a></code>）</h3>\n\n<p>上下文压缩时用模型生成结构化摘要，五个字段：<code class=\"inline\">task_overview</code> / <code class=\"inline\">current_state</code> / <code class=\"inline\">important_discoveries</code> / <code class=\"inline\">next_steps</code> / <code class=\"inline\">context_to_preserve</code>。W07 会精读，本周知道\"压缩不是简单截断，而是结构化总结\"即可。</p>\n\n<h3>3.6 不花钱学用法：<code class=\"inline\"><a class=\"code-link code-link-missing\" data-code=\"tests-agent-basic-test-py\" data-ref=\"tests/agent_basic_test.py\">tests/agent_basic_test.py</a></code></h3>\n\n<p>源码里这个测试文件用 <code class=\"inline\">MockModel</code>/<code class=\"inline\">MockTool</code> 假数据测 Agent，<strong>不调真 API</strong>。e YAGNI：想读内核又舍不得 token，就照着它改写示例。</p>\n\n<h2>4. 动手作业</h2>\n\n<p>放 <code class=\"inline\">code/w04/</code>。</p>\n\n<h3>作业 1：跑通 quickstart + 看懂事件流</h3>\n\n<p><code class=\"inline\"><a class=\"code-link\" data-code=\"code-w04-quickstart-py\" data-ref=\"code/w04/quickstart.py\">code/w04/quickstart.py</a></code>：</p>\n\n<pre><code class=\"language-python\">\n# code/w04/quickstart.py\n# 目标：用 agentscope 2.0 跑最小 Agent，观察事件流事件的种类\nimport asyncio\nimport os\n\nfrom agentscope.agent import Agent, ReActConfig\nfrom agentscope.credential import DashScopeCredential\nfrom agentscope.event import EventType\nfrom agentscope.message import UserMsg\nfrom agentscope.model import DashScopeChatModel\nfrom agentscope.tool import Bash, Read, Toolkit\n\n\nasync def main():\n    model = DashScopeChatModel(\n        credential=DashScopeCredential(api_key=os.environ[&quot;DASHSCOPE_API_KEY&quot;]),\n        model=&quot;qwen-plus&quot;,\n        stream=True,\n    )\n    agent = Agent(\n        name=&quot;demo&quot;,\n        system_prompt=&quot;你是助手，可执行命令。&quot;,\n        model=model,\n        toolkit=Toolkit(tools=[Bash(), Read()]),\n        react_config=ReActConfig(max_iters=10),  # 显式限步\n    )\n\n    counts: dict[str, int] = {}\n    async for evt in agent.reply_stream(UserMsg(&quot;user&quot;, &quot;列出当前目录下的 .md 文件数量。&quot;)):\n        t = evt.type.value\n        counts[t] = counts.get(t, 0) + 1\n        # 文字增量实时打印\n        if evt.type == EventType.TEXT_BLOCK_DELTA:\n            print(evt.text_delta, end=&quot;&quot;, flush=True)\n        # 工具调用结束时通报\n        elif evt.type == EventType.TOOL_CALL_END:\n            print(f&quot;\\n  ⚙️ 调用工具: {getattr(evt, &#x27;name&#x27;, &#x27;?&#x27;)}&quot;)\n    print(&quot;\\n\\n事件统计:&quot;, counts)\n\n\nif __name__ == &quot;__main__&quot;:\n    asyncio.run(main())\n\n</code></pre>\n\n<p>跑：<code class=\"inline\">python <a class=\"code-link\" data-code=\"code-w04-quickstart-py\" data-ref=\"code/w04/quickstart.py\">code/w04/quickstart.py</a></code></p>\n\n<p><strong>预期</strong>：Agent 调 <code class=\"inline\">Bash</code> 跑 <code class=\"inline\">ls</code>/<code class=\"inline\">find</code>，吐出 <code class=\"inline\">.md</code> 数量。结尾事件统计里你会看到 <code class=\"inline\">text_block_delta</code>、<code class=\"inline\">tool_call_end</code>、<code class=\"inline\">reply_start</code>、<code class=\"inline\">reply_end</code> 等若干种。<strong>这就是 2.0 的\"事件流\"心智</strong>——Agent 的整个生命周期是一串事件。</p>\n\n<h3>作业 2：对照 W03，列差异表</h3>\n\n<p>开 <code class=\"inline\"><a class=\"code-link code-link-missing\" data-code=\"code-w04-diff-md\" data-ref=\"code/w04/diff.md\">code/w04/diff.md</a></code>，把你 W03 的 <code class=\"inline\"><a class=\"code-link code-link-missing\" data-code=\"react-handcraft-py\" data-ref=\"react_handcraft.py\">react_handcraft.py</a></code> 和 <code class=\"inline\">Agent._reply_impl</code> 的步骤逐行对照，写清\"我做了什么 / 框架多了什么\"。这是面试讲项目的神来之笔。</p>\n\n<h3>作业 3：用 <code class=\"inline\">reply()</code> 拿最终消息</h3>\n\n<p>把上面改成 <code class=\"inline\">msg = await agent.reply(UserMsg(...))</code>，打印 <code class=\"inline\">msg.usage</code>（token 用量）和 <code class=\"inline\">type(msg)</code>。体会\"流式拿过程、reply 拿结果\"两条路。</p>\n\n<h2>5. 面试问答卡</h2>\n\n<p><strong>Q1：agentscope 2.0 为什么只剩一个 <code class=\"inline\">Agent</code> 类？</strong></p>\n<ul>\n<li>参考答案：现代 LLM 自带推理和工具使用能力，框架不该用类层次固定行为。2.0 用<strong>组合</strong>代替<strong>继承</strong>：一个统一 <code class=\"inline\">Agent</code> 内置 reasoning-acting 循环，行为差异靠 plug 不同 model/toolkit/middlewares/config 表达。这比 1.0 的 ReActAgent/DialogAgent 子类更灵活、更易扩展。</li>\n<li>源码佐证：<code class=\"inline\">Agent.__init__</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"agent-agent-py-100\" data-ref=\"agent/_agent.py:100\">agent/_agent.py:100</a></code>）装配车间；middlewares 按钩子筛选（<code class=\"inline\">:170-188</code>）。</li>\n<li>话术：「LLM 自己会推理了，框架不用类来规定行为，改成组合——plug 什么就是什么 Agent。」</li>\n</ul>\n\n<p><strong>Q2：<code class=\"inline\">reply_stream</code> 和 <code class=\"inline\">reply</code> 有什么区别？</strong></p>\n<ul>\n<li>参考答案：<code class=\"inline\">reply_stream</code>（<code class=\"inline\">:194</code>）返回 <code class=\"inline\">AsyncGenerator[AgentEvent]</code>，边跑边吐事件，UI 能实时渲染，是 2.0 主力入口；<code class=\"inline\">reply</code>（<code class=\"inline\">:225</code>）内部 drain 整个流、返回最终 <code class=\"inline\">Msg</code>，阻塞到完成。<code class=\"inline\">reply_stream</code> 内部就是 <code class=\"inline\">async for chunk in self._reply(...)</code> 只 yield 非 Msg 项（<code class=\"inline\">:221</code>）。</li>\n<li>话术：「stream 拿过程给 UI 渲染，reply 拿最终结果。」</li>\n</ul>\n\n<p><strong>Q3：<code class=\"inline\">_reply_impl</code> 的循环和手写 ReAct 有什么对应？</strong></p>\n<ul>\n<li>参考答案：<code class=\"inline\">_check_next_action</code>（<code class=\"inline\">:2542</code>）≈ 手写的\"tool_calls 为空就结束\"；<code class=\"inline\">_reasoning</code>（<code class=\"inline\">:916</code>）≈ 调模型；<code class=\"inline\">_execute_*_tool_calls</code>（<code class=\"inline\">:1375/:1425</code>）≈ 执行+回填。框架额外做了并发分批、上下文压缩（<code class=\"inline\">:759</code>）、事件流、人在回路。</li>\n<li>话术：「和我手写的轮子一一对应，框架额外加了并发、压缩、事件、HITL。」</li>\n</ul>\n\n<p><strong>Q4：<code class=\"inline\">max_iters</code>、<code class=\"inline\">trigger_ratio</code>、<code class=\"inline\">tool_result_limit</code> 分别治什么病？</strong></p>\n<ul>\n<li>参考答案：<code class=\"inline\">max_iters</code>（<code class=\"inline\">ReActConfig</code>）防死循环；<code class=\"inline\">trigger_ratio=0.8</code>（<code class=\"inline\">ContextConfig</code>）上下文超 80% 触发压缩；<code class=\"inline\">tool_result_limit=50000</code> 工具结果超 5 万 token 截断——分别治死循环、上下文爆炸、工具结果过长，正好是 W03 手写撞上的三个坑。</li>\n<li>话术：「三个参数治三个病：死循环、上下文爆、工具结果过长。」</li>\n</ul>\n\n<h2>6. 从 1.0 到 2.0 / 避坑</h2>\n\n<ul>\n<li>1.0：<code class=\"inline\">from agentscope.agents import ReActAgent, DialogAgent</code> → 2.0：<code class=\"inline\">from agentscope.agent import Agent</code>（<strong>模块名单数</strong>，糟点：极容易拼错）。</li>\n<li>1.0：<code class=\"inline\">agent(msg)</code> 同步 → 2.0：<code class=\"inline\">await agent.reply_stream(msg)</code> 异步。</li>\n<li>1.0：行为=子类 → 2.0：行为=组合（plug toolkit/middlewares/config）。</li>\n<li>旧资料里 <code class=\"inline\">from agentscope.pipelines import ...</code> 整个模块在 2.0 已删除，别找了。</li>\n</ul>\n\n<h2>附：本周 checkpoint</h2>\n\n<ul>\n<li>[ ] quickstart 跑通，看到事件统计里多种事件类型</li>\n<li>[ ] <code class=\"inline\">reply()</code> 跑通，打印出 <code class=\"inline\">msg.usage</code></li>\n<li>[ ] W03 轮子 vs <code class=\"inline\">_reply_impl</code> 差异表写好</li>\n<li>[ ] 能讲清\"组合 > 继承\"在 2.0 的体现</li>\n</ul>\n\n<p>---</p>\n<p>下周：<a href=\"W05-消息Event与模型对接.md\">W05 消息/Event 与模型对接</a>——把 <code class=\"inline\">Msg</code>/ContentBlock/事件流/多 provider 接通。</p>", "toc": [{"level": 1, "text": "W04 · AgentScope 入门与 Agent 内核"}, {"level": 2, "text": "1. 本周你将搞懂什么"}, {"level": 2, "text": "2. 原理铺垫：2.0 的设计哲学"}, {"level": 2, "text": "3. 源码精读"}, {"level": 3, "text": "3.1 构造与装配（`_agent.py:100`）"}, {"level": 3, "text": "3.2 三个入口方法（`:194 / :225 / :266`）"}, {"level": 3, "text": "3.3 内核循环 `_reply_impl`（`:664`）"}, {"level": 3, "text": "3.4 三个 Config（`agent/_config.py`）"}, {"level": 3, "text": "3.5 SummarySchema（`_config.py:9`）"}, {"level": 3, "text": "3.6 不花钱学用法：`tests/agent_basic_test.py`"}, {"level": 2, "text": "4. 动手作业"}, {"level": 3, "text": "作业 1：跑通 quickstart + 看懂事件流"}, {"level": 1, "text": "code/w04/quickstart.py"}, {"level": 1, "text": "目标：用 agentscope 2.0 跑最小 Agent，观察事件流事件的种类"}, {"level": 3, "text": "作业 2：对照 W03，列差异表"}, {"level": 3, "text": "作业 3：用 `reply()` 拿最终消息"}, {"level": 2, "text": "5. 面试问答卡"}, {"level": 2, "text": "6. 从 1.0 到 2.0 / 避坑"}, {"level": 2, "text": "附：本周 checkpoint"}], "prev": "W03-手写ReAct与Plan-Execute循环", "next": "W05-消息Event与模型对接", "path": "阶段二-框架/W04-agent scope入门与Agent内核.md"};
