window.__docs__ = window.__docs__ || {};
window.__docs__["W01-Python异步与LLM-API调用"] = {"doc_id": "W01-Python异步与LLM-API调用", "title": "W01 · Python 异步与 LLM API 调用", "stage": "地基", "html": "<h1>W01 · Python 异步与 LLM API 调用</h1>\n\n<p>> 本周目标 | 打通 asyncio 心智模型，能用原生 SDK 流式调通 LLM，并手写一个 SSE 字节流解析器。</p>\n<p>> JD 考点：Python 异步编程基础、LLM API 流式调用、Function Calling 预备。</p>\n\n<h2>1. 本周你将搞懂什么</h2>\n\n<p>agentscope 2.0 <strong>从头到尾是异步的</strong>——<code class=\"inline\">Agent.reply_stream()</code> 是个 <code class=\"inline\">AsyncGenerator</code>，模型 <code class=\"inline\">__call__</code> 是 <code class=\"inline\">async def</code>，工具并发执行靠 <code class=\"inline\">asyncio</code>。如果你对 asyncio 还停留在\"知道有这东西\"，本周就会被代码每一个角落卡住。</p>\n\n<p>本周不碰框架的高级概念，只做一件事：<strong>把异步和流式彻底搞懂</strong>，顺带用原生方式把几个主流大模型 API 调通，为 W03 手写 ReAct 循环铺路。</p>\n\n<p>学完能答：</p>\n<ul>\n<li>为什么要异步？协程比线程好在哪？</li>\n<li><code class=\"inline\">async for</code> 是什么？为什么 LLM 要\"流式\"返回？</li>\n<li>SSE 是什么？字节流怎么变成一行行 token？</li>\n<li>多个模型请求怎么并发跑、怎么等一起回来？</li>\n</ul>\n\n<h2>2. 原理铺垫</h2>\n\n<h3>2.1 asyncio 心智模型</h3>\n\n<pre><code class=\"language-\">\n传统同步：你点外卖→盯着骑手→拿到→继续干   (线程被&quot;阻塞&quot;，闲着等)\n异步：    你点外卖→去做别的→骑手到了通知你  (事件循环帮你&quot;等&quot;，CPU 去干别的)\n\n</code></pre>\n\n<p>关键三件套：</p>\n\n<ul>\n<li><code class=\"inline\">async def</code> 定义的函数是<strong>协程函数</strong>，调用它返回一个<strong>协程对象</strong>，不会立刻执行——必须被 <code class=\"inline\">await</code> 或放进事件循环。</li>\n<li><code class=\"inline\">await</code> = \"我在等某个异步操作，事件循环你先去调度别人\"。</li>\n<li><code class=\"inline\">asyncio.run(coro)</code> = 启动事件循环，跑一个协程直到结束（程序入口用一次）。</li>\n</ul>\n\n<pre><code class=\"language-python\">\nimport asyncio\n\nasync def say(name, delay):\n    await asyncio.sleep(delay)   # 模拟 IO 等待，让出控制权\n    return f&quot;{name} done in {delay}s&quot;\n\nasync def main():\n    # 并发：两个协程同时&quot;等&quot;，总耗时约 max(1,2)=2s，而不是 3s\n    r = await asyncio.gather(say(&quot;A&quot;, 1), say(&quot;B&quot;, 2))\n    print(r)  # [&#x27;A done in 1s&#x27;, &#x27;B done in 2s&#x27;]\n\nasyncio.run(main())\n\n</code></pre>\n\n<p>经验法则：<strong>IO 密集（等网络/磁盘/模型）用异步</strong>；CPU 密集（大计算）用多进程。调 LLM 就是典型的\"等网络\"，异步最合适——一个进程能同时等几十个请求。</p>\n\n<h3>2.2 LLM 为什么要\"流式\"</h3>\n\n<p>生成式模型是一个 token 一个 token 吐出来的。如果等整句生成完再返回，用户要干等几秒到几十秒才看到第一个字；流式则边生成边推，首字延迟降到几百毫秒。</p>\n\n<p>两种获取方式：</p>\n<ul>\n<li><strong>非流式</strong>：一次 <code class=\"inline\">POST</code>，等模型生成完，返回完整 JSON。代码简单，体验差。</li>\n<li><strong>流式（SSE）</strong>：服务器用 <code class=\"inline\">text/event-stream</code> 一条条推 <code class=\"inline\">data: {...}</code> 块，客户端边收边解析。</li>\n</ul>\n\n<h3>2.3 SSE 协议长什么样</h3>\n\n<p>SSE（Server-Sent Events）本质是 HTTP 长连接上，服务器按这个格式推文本：</p>\n\n<pre><code class=\"language-\">\ndata: {&quot;choices&quot;:[{&quot;delta&quot;:{&quot;content&quot;:&quot;你&quot;}}]}\n\ndata: {&quot;choices&quot;:[{&quot;delta&quot;:{&quot;content&quot;:&quot;好&quot;}}]}\n\ndata: [DONE]\n\n</code></pre>\n\n<ul>\n<li>每条消息以 <code class=\"inline\">data: </code> 开头，以<strong>两个换行</strong>分隔。</li>\n<li>字节流可能在你收到时被切断（\"半个 JSON\"），所以解析时要维护一个缓冲区，按行/按分隔符拼。</li>\n</ul>\n\n<h2>3. 源码精读</h2>\n\n<p>agentscope 的模型入口是 <code class=\"inline\">ChatModelBase.__call__</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"src-agentscope-model-base-py-157\" data-ref=\"src/agentscope/model/_base.py:157\">src/agentscope/model/_base.py:157</a></code>，<code class=\"inline\">async def</code>）。它把一次模型调用抽象成 <code class=\"inline\">await model(...)</code>，流式时返回异步生成器。</p>\n\n<p>DashScope 模型 <code class=\"inline\">DashScopeChatModel._call_api</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"model-dashscope-model-py-176\" data-ref=\"model/_dashscope/_model.py:176\">model/_dashscope/_model.py:176</a></code>）内部用 OpenAI 兼容 SDK：构造 <code class=\"inline\">openai.AsyncClient(api_key=..., base_url=...)</code>，然后 <code class=\"inline\">await client.chat.completions.create(stream=True, ...)</code>——这正是你本周要手写的底层。</p>\n\n<p><code class=\"inline\">count_tokens</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"base-py-350\" data-ref=\"_base.py:350\">_base.py:350</a></code>）和 <code class=\"inline\">generate_structured_output</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"base-py-438\" data-ref=\"_base.py:438\">_base.py:438</a></code>）是后面 W07/W05 会用到的，本周先知道有这两个口子。</p>\n\n<p>> 本周不精读框架，重点是<strong>自己用 httpx 把这条流式链路手写一遍</strong>，这样后面看框架源码时才不会觉得\"这堆 async for 是天书\"。</p>\n\n<h2>4. 动手作业</h2>\n\n<p>代码放在 <code class=\"inline\">2027年/code/w01/</code>。三个小任务，逐个加难度。</p>\n\n<h3>作业 1：手写 SSE 流式解析器（不依赖 SDK）</h3>\n\n<p><code class=\"inline\"><a class=\"code-link\" data-code=\"code-w01-sse-handwrite-py\" data-ref=\"code/w01/sse_handwrite.py\">code/w01/sse_handwrite.py</a></code>：</p>\n\n<pre><code class=\"language-python\">\n# code/w01/sse_handwrite.py\n# 目标：用 httpx 直接调 DashScope OpenAI 兼容接口，自己解析 SSE 字节流\nimport asyncio\nimport json\nimport os\n\nimport httpx\n\nDASHSCOPE_URL = &quot;https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions&quot;\n\n\nasync def stream_chat(prompt: str) -&gt; None:\n    headers = {\n        &quot;Authorization&quot;: f&quot;Bearer {os.environ[&#x27;DASHSCOPE_API_KEY&#x27;]}&quot;,\n        &quot;Content-Type&quot;: &quot;application/json&quot;,\n    }\n    payload = {\n        &quot;model&quot;: &quot;qwen-plus&quot;,\n        &quot;messages&quot;: [{&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: prompt}],\n        &quot;stream&quot;: True,\n        &quot;stream_options&quot;: {&quot;include_usage&quot;: True},\n    }\n\n    # async with：上下文管理，离开块自动关闭连接\n    async with httpx.AsyncClient(timeout=60) as client:\n        async with client.stream(&quot;POST&quot;, DASHSCOPE_URL, headers=headers, json=payload) as resp:\n            resp.raise_for_status()\n            buf = &quot;&quot;  # 缓冲区：处理&quot;半行&quot;\n            # iter_raw 给字节，按 SSE 协议逐块拼\n            async for raw in resp.aiter_text():\n                buf += raw\n                # SSE 以两个换行分隔每条事件\n                while &quot;\\n\\n&quot; in buf:\n                    chunk, buf = buf.split(&quot;\\n\\n&quot;, 1)\n                    for line in chunk.splitlines():\n                        if not line.startswith(&quot;data:&quot;):\n                            continue\n                        data = line[len(&quot;data:&quot;):].strip()\n                        if data == &quot;[DONE]&quot;:\n                            print(&quot;\\n[DONE]&quot;)\n                            return\n                        delta = json.loads(data)[&quot;choices&quot;][0].get(&quot;delta&quot;, {})\n                        if &quot;content&quot; in delta:\n                            print(delta[&quot;content&quot;], end=&quot;&quot;, flush=True)\n\n\nasync def main():\n    await stream_chat(&quot;用三句话向我解释什么是协程。&quot;)\n\n\nif __name__ == &quot;__main__&quot;:\n    asyncio.run(main())\n\n</code></pre>\n\n<p>跑：<code class=\"inline\">python <a class=\"code-link\" data-code=\"code-w01-sse-handwrite-py\" data-ref=\"code/w01/sse_handwrite.py\">code/w01/sse_handwrite.py</a></code></p>\n\n<p><strong>预期</strong>：模型解释协程的三个句子<strong>逐字</strong>出现，最后打印 <code class=\"inline\">[DONE]</code>。这个手感就是流式。</p>\n\n<h3>作业 2：并发调 3 个 provider 对比</h3>\n\n<p><code class=\"inline\"><a class=\"code-link\" data-code=\"code-w01-concurrent-providers-py\" data-ref=\"code/w01/concurrent_providers.py\">code/w01/concurrent_providers.py</a></code>：把同样的问题并发丢给 3 个模型（或同模型 3 次），用 <code class=\"inline\">asyncio.gather</code> 等全部回来，打印各自耗时和首句。</p>\n\n<pre><code class=\"language-python\">\n# code/w01/concurrent_providers.py\nimport asyncio\nimport os\nimport time\n\nimport httpx\n\nURL = &quot;https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions&quot;\n\n\nasync def call_once(label: str, model: str, prompt: str) -&gt; str:\n    headers = {&quot;Authorization&quot;: f&quot;Bearer {os.environ[&#x27;DASHSCOPE_API_KEY&#x27;]}&quot;}\n    payload = {&quot;model&quot;: model, &quot;messages&quot;: [{&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: prompt}]}\n    t0 = time.perf_counter()\n    async with httpx.AsyncClient(timeout=60) as client:\n        r = await client.post(URL, headers=headers, json=payload)\n        r.raise_for_status()\n        text = r.json()[&quot;choices&quot;][0][&quot;message&quot;][&quot;content&quot;]\n    print(f&quot;[{label}] {time.perf_counter()-t0:.2f}s -&gt; {text[:40]}&quot;)\n    return text\n\n\nasync def main():\n    prompt = &quot;一句话回答：1+1 等于几？&quot;\n    # 三个协程并发，总耗时 ≈ 最慢的那个，而不是三者之和\n    await asyncio.gather(\n        call_once(&quot;qwen-plus&quot;, &quot;qwen-plus&quot;, prompt),\n        call_once(&quot;qwen-turbo&quot;, &quot;qwen-turbo&quot;, prompt),\n        call_once(&quot;qwen-plus-2&quot;, &quot;qwen-plus&quot;, prompt),\n    )\n\n\nif __name__ == &quot;__main__&quot;:\n    asyncio.run(main())\n\n</code></pre>\n\n<p><strong>预期</strong>：三行几乎同时打印，总耗时接近单次最慢者。把 <code class=\"inline\">asyncio.gather</code> 换成顺序 <code class=\"inline\">await</code> 三次你会看到总耗时变成三者之和——这就是并发的价值。</p>\n\n<h3>作业 3：事件循环嵌套避坑（选做）</h3>\n\n<p>如果在 Jupyter / 已有事件循环的环境里直接 <code class=\"inline\">asyncio.run</code> 会报 \"event loop already running\"。体会一下：在交互式环境里要么用 <code class=\"inline\">await main()</code>，要么用 <code class=\"inline\">nest_asyncio</code>。知道这个坑存在即可，教程示例一律用脚本 <code class=\"inline\">asyncio.run</code>。</p>\n\n<h2>5. 面试问答卡</h2>\n\n<p><strong>Q1：协程和线程的区别？为什么调 LLM 用协程而不是线程？</strong></p>\n<ul>\n<li>参考答案：线程是 OS 调度的抢占式并发，切换有内核开销，且共享内存要加锁；协程是用户态协作式并发，在 IO 等待点 <code class=\"inline\">await</code> 主动让出，切换轻量、单线程内无数据竞争。调 LLM 是 IO 密集（等网络），协程用\"等待时间\"跑别的请求，几千并发只占一个线程，比开几千个线程省内存。</li>\n<li>源码佐证：agentscope <code class=\"inline\">ChatModelBase.__call__</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"model-base-py-157\" data-ref=\"model/_base.py:157\">model/_base.py:157</a></code>）即 <code class=\"inline\">async def</code>，<code class=\"inline\">Agent.reply_stream</code>（<code class=\"inline\"><a class=\"code-link\" data-code=\"agent-agent-py-194\" data-ref=\"agent/_agent.py:194\">agent/_agent.py:194</a></code>）返回 <code class=\"inline\">AsyncGenerator</code>，全程协程。</li>\n<li>一句话话术：「调模型就是等网络，协程在等的时候去跑别的请求，单进程撑高并发还不用加锁。」</li>\n</ul>\n\n<p><strong>Q2：什么是 SSE？和 WebSocket 有什么区别？</strong></p>\n<ul>\n<li>参考答案：SSE 是服务器单向推送的 HTTP 长连接，格式是 <code class=\"inline\">data: ...\\n\\n</code>，浏览器和 httpx 都内置支持，断线自动重连；WebSocket 是双向全双工，需握手升级，协议更重。LLM 流式输出只需\"服务器→客户端\"单向推，SSE 足够且更简单。</li>\n<li>一句话话术：「LLM 流式只需要服务器单向吐 token，SSE 比 WebSocket 轻得多，还自带断线重连。」</li>\n</ul>\n\n<p><strong>Q3：<code class=\"inline\">asyncio.gather</code> 和顺序 await 有什么区别？</strong></p>\n<ul>\n<li>参考答案：顺序 <code class=\"inline\">await a; await b</code> 是串行，总耗时 <code class=\"inline\">a+b</code>；<code class=\"inline\">gather(a,b)</code> 让两个协程并发跑，总耗时 <code class=\"inline\">max(a,b)</code>。前提是 <code class=\"inline\">a</code>、<code class=\"inline\">b</code> 内部有 <code class=\"inline\">await</code> 让出点（如 IO），否则协程不会真正并行。</li>\n<li>一句话话术：「gather 是并发等，顺序 await 是排队等。」</li>\n</ul>\n\n<h2>6. 从 1.0 到 2.0 / 避坑</h2>\n\n<ul>\n<li><strong>1.0</strong>：<code class=\"inline\">agent(msg)</code> 是同步阻塞调用，一行拿到结果。</li>\n<li><strong>2.0</strong>：全异步，<code class=\"inline\">await agent.reply_stream(msg)</code> 返回事件流，<code class=\"inline\">await agent.reply(msg)</code> 才阻塞到拿到最终 <code class=\"inline\">Msg</code>。</li>\n<li>你在网上搜到的 <code class=\"inline\">from agentscope.agents import ReActAgent</code> 之类的 1.0 写法<strong>全部失效</strong>，2.0 是 <code class=\"inline\">from agentscope.agent import Agent</code>（注意是单数 <code class=\"inline\">agent</code> 模块）。</li>\n</ul>\n\n<h2>附：本周 checkpoint</h2>\n\n<ul>\n<li>[ ] 能解释协程/线程区别，不看资料</li>\n<li>[ ] 作业 1 跑通，看到 token 逐字流式打印</li>\n<li>[ ] 作业 2 跑通，亲眼看到并发比串行快</li>\n<li>[ ] 知道为什么 <code class=\"inline\">asyncio.run</code> 在 Jupyter 里会报错</li>\n</ul>\n\n<p>---</p>\n<p>下周：<a href=\"W02-Agent范式与Prompt工程.md\">W02 Agent 范式与 Prompt 工程</a></p>", "toc": [{"level": 1, "text": "W01 · Python 异步与 LLM API 调用"}, {"level": 2, "text": "1. 本周你将搞懂什么"}, {"level": 2, "text": "2. 原理铺垫"}, {"level": 3, "text": "2.1 asyncio 心智模型"}, {"level": 3, "text": "2.2 LLM 为什么要\"流式\""}, {"level": 3, "text": "2.3 SSE 协议长什么样"}, {"level": 2, "text": "3. 源码精读"}, {"level": 2, "text": "4. 动手作业"}, {"level": 3, "text": "作业 1：手写 SSE 流式解析器（不依赖 SDK）"}, {"level": 1, "text": "code/w01/sse_handwrite.py"}, {"level": 1, "text": "目标：用 httpx 直接调 DashScope OpenAI 兼容接口，自己解析 SSE 字节流"}, {"level": 3, "text": "作业 2：并发调 3 个 provider 对比"}, {"level": 1, "text": "code/w01/concurrent_providers.py"}, {"level": 3, "text": "作业 3：事件循环嵌套避坑（选做）"}, {"level": 2, "text": "5. 面试问答卡"}, {"level": 2, "text": "6. 从 1.0 到 2.0 / 避坑"}, {"level": 2, "text": "附：本周 checkpoint"}], "prev": "README", "next": "W02-Agent范式与Prompt工程", "path": "阶段一-地基/W01-Python异步与LLM-API调用.md"};
