# 06 开发任务清单（M1 最小闭环）

> 状态图例：`[ ]` 待办 ／ `[~]` 进行中 ／ `[x]` 完成
> 本清单随编码进度持续更新，更新记录见文末。

## 一、agent-rag-python（FastAPI AI 层）

- [x] 1. 项目骨架：requirements.txt、config（pydantic-settings）、日志、Internal Token 校验、main.py
- [x] 2. schemas 契约：chat / retrieve / ingest / kb（对齐《03-服务接口设计》）
- [x] 3. 文档解析器：pdf（PyMuPDF + 页眉页脚清洗）、docx、md/txt，统一 ParserOutput
- [x] 4. 切分器：递归字符切分 + 结构感知切分（标题路径 metadata）、可配 chunk_size/overlap
- [x] 5. Embedder：BGE-M3（稠密+稀疏），懒加载、hash 缓存、query 指令前缀；FakeEmbedder 联调模式
- [x] 6. VectorStore 抽象 + Milvus 实现（collection 初始化、稠密+稀疏混合检索、按 doc 删除）+ InMemory 联调实现
- [x] 7. Retriever：双路召回 + RRF 融合 + kb 过滤
- [x] 8. Reranker：bge-reranker-v2-m3，sigmoid 归一化、阈值判断、故障降级
- [x] 9. Generator：query 改写（多轮指代消解）、Prompt 组装（编号引用 + token 预算）、OpenAI 兼容流式
- [x] 10. API 路由：POST /v1/chat/completions（SSE）、/v1/retrieve、/v1/ingest（异步+回调）、/v1/documents/delete、/v1/kb/*、/healthz
- [x] 11. 工程件：Dockerfile、.env.example、README（启动说明）

> 验证：compileall 全量语法通过；结构切分/递归切分/RRF/内存库链路/断行修复运行测试通过；无模型模式服务冒烟（healthz、retrieve、401 鉴权）通过。

## 二、agent-rag-java（Spring Boot 业务层）

- [x] 1. 项目骨架：pom.xml（Boot 3.3 + MyBatis-Plus + jjwt + minio）、application.yml、schema.sql
- [x] 2. 通用件：统一返回体、全局异常处理、CORS、Security 配置（无状态 JWT）
- [x] 3. 认证：登录接口 + JWT 签发/校验过滤器 + DataInitializer 初始管理员（admin/admin123）
- [x] 4. 知识库模块：kb CRUD（M1 全量开放，kb_permission 表已建、二期过滤）
- [x] 5. 文档模块：MinIO 上传（sha256 去重）、列表/删除、pending→parsing→success/failed 状态机、/internal/callback/ingest 回调
- [x] 6. AiClient：Python 服务封装（REST + SSE 逐行读取 + Internal Token + 预签名 URL）
- [x] 7. 会话模块：conversation/message CRUD、问答 SSE 透传、done 时消息落库（含 citations/retrieval_debug/改写后 query）、反馈接口
- [x] 8. 工程件：Dockerfile（两阶段）、README、.gitignore

> 编译验证：见第四部分第 3 项（Maven 安装完成后执行）。

## 三、agent-rag-front（React 前端）

- [x] 1. 项目骨架：Vite + React 18 + TS、package.json、vite 代理、tsconfig
- [x] 2. 基础设施：fetch 封装（JWT 注入/401 跳登录）、SSE 客户端（POST + ReadableStream）、Zustand auth store
- [x] 3. 登录页：表单 + token 持久化
- [x] 4. 知识库页：知识库列表/新建、文档上传、parse_status 3s 轮询刷新、失败重试、删除
- [x] 5. 对话页：知识库多选、会话列表、SSE 流式渲染（react-markdown + 高亮）、引用 Tag 面板、拒答态、停止生成
- [x] 6. 工程件：Dockerfile（nginx）、nginx.conf（/api 反代 + SSE 关缓冲）、README

> 验证：`tsc --noEmit` 类型检查 + `vite build` 构建通过（dist 产物 445KB gzip）。

## 四、联调与验证

- [x] 1. Python 端语法/导入验证（compileall + 核心逻辑运行测试 + 无模型服务冒烟 + pytest 5 项）
- [x] 2. 前端构建验证（npm install + tsc --noEmit + vite build 通过）
- [~] 3. Java 编译验证（Maven 安装中，装好后 mvn -q -DskipTests package）
- [ ] 4. 端到端冒烟（docker-compose 环境，人工）

---

## 更新记录

- 2026-07-26 22:24 清单创建（M1 范围，依据《05-部署与开发路线图》M1/M2 项）
- 2026-07-26 22:40 agent-rag-python 全部完成并通过验证（含 FakeEmbedder/InMemoryStore 无模型联调链路）
- 2026-07-26 23:05 agent-rag-front 全部完成，构建通过；根目录新增 docker-compose.yml 与总 README；pytest 5 项通过
- 2026-07-26 23:05 agent-rag-java 代码全部完成（认证/知识库/文档/AiClient/会话SSE），待 Maven 就绪后编译验证
